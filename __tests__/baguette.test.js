const baguette = require("../baguette");

describe("baguette", () => {
  describe("Creating a new component", () => {
    it("Looks up the config from user’s .baguetterrrc file", () => {
      baguette({ name: "NewComponent" });
      expect();
    });
  });
});
